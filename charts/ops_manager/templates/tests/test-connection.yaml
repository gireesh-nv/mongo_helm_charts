apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ops_manager.fullname" . }}-test-connection"
  labels:
    {{- include "ops_manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ops_manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
