apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise_operator.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise_operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise_operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
