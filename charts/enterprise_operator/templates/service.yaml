apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise_operator.fullname" . }}
  labels:
    {{- include "enterprise_operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enterprise_operator.selectorLabels" . | nindent 4 }}
