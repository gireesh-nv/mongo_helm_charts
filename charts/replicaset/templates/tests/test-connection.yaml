apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "replicaset.fullname" . }}-test-connection"
  labels:
    {{- include "replicaset.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "replicaset.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
