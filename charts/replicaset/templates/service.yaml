apiVersion: v1
kind: Service
metadata:
  name: {{ include "replicaset.fullname" . }}
  labels:
    {{- include "replicaset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "replicaset.selectorLabels" . | nindent 4 }}
